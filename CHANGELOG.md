# V1.2.9 校验与版本一致性修复

- 修复 `scripts/validate_skill.py` 将版本硬编码为 1.2.6 导致新版 Skill 假失败的问题；校验器现自动读取根目录 `VERSION` 并校验语义化版本格式。
- 校验器同步采用 V1.2.9 股票图片协议实际措辞，移除已废弃的“下载附件链接”旧字段要求。
- `modules/commerce/README.md` 去除旧版 V1.2.6 硬编码，改为以当前 `VERSION` 对应协议为准。
- 补齐 V1.2.8 的发行说明，并将股票图片协议标题与当前版本同步。
- 本补丁不改变奇门取用、定性、应期或股票十日预测逻辑。

# V1.2.8 股票图片渲染修复版

- 修复图片协议要求16:9、但官方模板与参考渲染器实际为1536×1024（3:2）的内部冲突；统一改为1600×900。
- 重写 `scripts/render_stock_dashboard.py`：渲染前强制验证十个交易日、唯一日期、五级方向与 rhythm_score 一致性。
- 六模块图片严格执行 `TEXT_FREEZE → IMAGE_PAYLOAD → 确定性程序化渲染 → 机械验收`，制图层不得重新读取九宫或二次推演。
- 十日曲线只按固定五级方向映射生成，并明确标注“相对强弱节奏，不是股价预测”。
- 模块三新增强制分支：有可靠现实价位才显示“关键价格区域”；否则自动显示“关键观察信号”。
- 强化正文优先：图片与冻结正文冲突时图片直接失败，不得发布，也不得触发重新分析。
- 发布前验收扩展为12项硬检查，禁止图片新增目标价、预测涨跌幅、仓位、杠杆、保证性结论或起局后倒灌信息。

# V1.2.7 股票预测时间轴冻结补丁

- 股票/ETF 十日预测的唯一时间锚点改为**奇门起局时间**，禁止按用户后续提问时间重新起算。
- 起局前现实行情、公告与市场状态用于“现实呼应核验”；核验数据截止到起局时点。
- 起局后、提问前已经发生的交易日仍属于原始 T+1～T+10 窗口，只能作为事后验证，不得反向污染原始预测。
- 十日窗口冻结后不得滚动：已发生日期保留原位置，未发生日期继续作为前瞻预测，不得删除已发生日期后再向后补足十个未来日。
- 明确盘中起局边界：默认从下一个实际交易日开始；只有问题在起局时明确包含当日剩余时段/当日收盘，才可纳入当日。
- 新增回归场景，拦截“按提问时间起算”“使用起局后行情反推”“滚动补足十个未来日”等错误。

# 更新记录

## 1.2.4 稳定化修订

- 新增分析指纹、固定证据账本、五问方向闸门和冻结顺序，专门处理同盘同问跨对话定性漂移。
- 没有盘面更正、主体/目标/范围变化、新局或规则版本变化时，同盘同问不得改变九级定性、主趋势和股票逐日原始方向。
- 股票明确区分卦象原始判断与现实执行层；行情、用户成本和持仓不得反向改写卦象结论。
- 股票默认交付锁定为两个部分：文字情况 + 六模块图片。
- 六模块固定为：当前行情、奇门十日节奏曲线、关键价格区域/观察信号、十日情况简表、卦象定性判断、总体结论&关键提示。
- 文字与图片共用同一冻结底稿；图片实行零推理和发布前逐字段一致性检查。
- 清理旧版完整七列表与新版三列表之间的输出优先级冲突。
- 合成场景基线提升至 182，新增 5 个跨对话稳定性场景。

## 1.2.4

- 股票/ETF默认用户输出压缩为：定性评价 + 十个实际交易日三列表 + 固定操作建议图；详细推演默认隐藏，用户要求时再展开。
- 十日简表固定为“日期｜可能走势｜建议”。
- 图片视觉模板锁定为横向16:9金融仪表盘。
- 图片新增【卦象定性判断】；九级等级必须复制冻结底稿。
- 新增零推理图片协议与发布前一致性校验。

历史版本详见 Git 提交记录。
